import React from "react"
import Layout from "../../components/Layout"

const kontakt = () => {
  const pageMeta = {
    title: "Simple Wires - kontakt",
    description: "description",
  }
  return <Layout pageMeta={pageMeta}>kontakt</Layout>
}

export default kontakt
