import React from "react"
import Layout from "@components/Layout"
const index = () => {
  const pageMeta = {
    title: "Simple Wires - Dla firmy",
    description: "dla firmy",
  }
  return <Layout pageMeta={pageMeta}></Layout>
}

export default index
