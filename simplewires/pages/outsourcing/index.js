import React from "react"
import Layout from "@components/Layout"
const index = () => {
  const pageMeta = {
    title: "Simple Wires - Outsourcing",
    description: "Outsourcing",
  }
  return <Layout pageMeta={pageMeta}></Layout>
}

export default index
